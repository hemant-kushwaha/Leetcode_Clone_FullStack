################################################################################
# Judge0 Configuration File
################################################################################
# This config is optimized for a low-resource laptop:
# - Single worker, minimal queue
# - Strict CPU (1s), memory (128MB), process limits (10)
# - Disabled network, compiler options, callbacks, etc.
# - Only C++, Java, JavaScript supported (languages filtered separately)
################################################################################

# 🧠 Worker Configuration
INTERVAL=1
COUNT=1
MAX_QUEUE_SIZE=10

# ⚙️ Execution Limits (STRICT)
CPU_TIME_LIMIT=1
MAX_CPU_TIME_LIMIT=2

CPU_EXTRA_TIME=0.5
MAX_CPU_EXTRA_TIME=1

WALL_TIME_LIMIT=3
MAX_WALL_TIME_LIMIT=5

MEMORY_LIMIT=128000
MAX_MEMORY_LIMIT=256000

STACK_LIMIT=32000
MAX_STACK_LIMIT=64000

MAX_PROCESSES_AND_OR_THREADS=10
MAX_MAX_PROCESSES_AND_OR_THREADS=20

MAX_FILE_SIZE=5120
MAX_MAX_FILE_SIZE=10240

NUMBER_OF_RUNS=1
MAX_NUMBER_OF_RUNS=1

# 🔒 Security + Performance
ENABLE_PER_PROCESS_AND_THREAD_TIME_LIMIT=false
ALLOW_ENABLE_PER_PROCESS_AND_THREAD_TIME_LIMIT=false

ENABLE_PER_PROCESS_AND_THREAD_MEMORY_LIMIT=false
ALLOW_ENABLE_PER_PROCESS_AND_THREAD_MEMORY_LIMIT=false

ALLOW_ENABLE_NETWORK=false
ENABLE_NETWORK=false

REDIRECT_STDERR_TO_STDOUT=true

# 🚫 Disable Heavy / Unused Features
ENABLE_COMPILER_OPTIONS=false
ENABLE_COMMAND_LINE_ARGUMENTS=false
ENABLE_BATCHED_SUBMISSIONS=false
ENABLE_CALLBACKS=false
ENABLE_ADDITIONAL_FILES=false

# ⚡ API Behavior (Simple + Fast)
ENABLE_WAIT_RESULT=true
MAX_SUBMISSION_BATCH_SIZE=5
SUBMISSION_CACHE_DURATION=1

# ⚙️ Rails (VERY IMPORTANT FOR CPU)
RAILS_MAX_THREADS=1
RAILS_SERVER_PROCESSES=1

# 🧾 Optional (Clean UX)
USE_DOCS_AS_HOMEPAGE=false

# 🔧 Database connections (keep defaults or adjust if needed)
REDIS_HOST=redis
REDIS_PASSWORD=uy89UJC2DnzQDPYF8Cuj7f2xJyCUtQM5

POSTGRES_HOST=db
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=NjEZ8eMKQHkMujZwmUpmEdYYFuJBmQRE

# 📡 Telemetry (optional – disable if you prefer)
JUDGE0_TELEMETRY_ENABLE=false

# 🛡️ Authentication & Authorization (disabled by default – left empty)
AUTHN_HEADER=
AUTHN_TOKEN=
AUTHZ_HEADER=
AUTHZ_TOKEN=

# 🌐 CORS (allow all – safe for local development)
ALLOW_ORIGIN=
DISALLOW_ORIGIN=
ALLOW_IP=
DISALLOW_IP=